# Copy this to .streamlit/secrets.toml — NEVER commit secrets.toml to GitHub

# Your OpenAI API key (required)
OPENAI_API_KEY = "sk-..."

# Password for regular users (leave blank for open access)
APP_PASSWORD = ""

# Separate password for YOUR admin panel — make this different and strong
# The Admin tab only appears in the sidebar when this is set
ADMIN_PASSWORD = "jyogi-admin-2024"
